# 测试执行报告 2026-06-22

> 契约：`tests/contracts/TEST-REPORT-CONTRACT.md`  
> 机器记录：`tests/results/latest-run.txt`

## 1. 元信息

| 项 | 值 |
|----|-----|
| 执行时间 | 2026-06-22T18:36:00+08:00 |
| 执行人 | Codex |
| 环境 | docker |
| API | http://localhost:8080 |
| 前端 | http://localhost:3000 |
| 脚本 | manual: npm run build; docker compose build/up; HTTP API probes |
| Git commit | unknown |
| 用例版本 | tests/cases/ @ 2026-06-22 |

## 2. 汇总

| 指标 | 数量 |
|------|------|
| PASS | 5 |
| FAIL | 0 |
| SKIP | 0 |
| BLOCK | 1 |
| 合计 | 6 |

**结论：** Docker 环境下模板图片读取与根据模板自动校正已通过联调验证；无产品 FAIL，剩余 BLOCK 为测试脚本默认 profile 的数据库初始化问题。

## 3. 通过标准对照

| 门槛 | 要求 | 结果 |
|------|------|------|
| P0 | 全部 PASS | 主要运行链路 PASS；`mvn test` 默认 profile 存在环境 BLOCK |
| P1 核心链 | AUTH-01 -> DS-01 -> DS-05 -> PRJ-04(success) -> RPT-01 | 本轮聚焦 TPL/ALG；AUTH 运行探针 200，完整脚本被 DB init BLOCK |
| P1 整体 | 核心模块无未预期 FAIL | 未发现 FAIL |

## 4. 分优先级结果

### P0 冒烟

| ID | 状态 | 说明 |
|----|------|------|
| SMK-03 | PASS | Docker `api` 镜像在 Maven/JDK17 下构建成功，`api`/`python` 已重启。 |

### P1 API - 认证

| ID | 状态 | 说明 |
|----|------|------|
| AUTH-01 | BLOCK | 运行时登录探针返回 200；但 `docker run maven:3.9-eclipse-temurin-17 mvn test` 的 SpringBootTest 被默认 profile DB init 脚本阻塞。 |

### P1 API - 模板 / 算法 / 项目 / 报告

| ID | 状态 | 说明 |
|----|------|------|
| TPL-02 | PASS | `GET /api/templates` 返回 200，旧丢图模板 `acb0320c-c312-4594-b701-4459b4d5d32a` 标记为 `imageAvailable=false`。 |
| TPL-03 | PASS | `GET /api/templates/033c48a9-f693-4036-a4eb-265d0880e352/image` 返回 200，`Content-Type=image/png`。 |
| ALG-03 | PASS | `POST /api/images/correction/align` 返回 200，`Content-Type=image/png`，输出 `tests/results/align-docker-output.png`，大小 843378 bytes。 |

### P2 前端手工

| ID | 状态 | 说明 |
|----|------|------|
| UI-08 | PASS | `npm run build` 成功，仅剩既有 `GroupListPage.jsx` hook dependency warning。 |

## 5. 失败与根因

| ID | 状态 | 现象 | 根因 | 建议 |
|----|------|------|------|------|
| AUTH-01 | BLOCK | Docker JDK17 下 `mvn test` 编译通过，但 SpringBootTest 启动失败 | 默认 profile 执行数据库初始化脚本失败；运行时 Docker profile 的登录探针实际为 200 | 给测试 profile 配置独立数据库/禁用 init，或改用 docker profile 执行后端集成测试 |

## 6. 测试过程发现

1. 可以在 Docker 环境下验证，本轮已完成 Docker `api`/`python` 镜像重建和运行时接口探针。
2. `acb0320c-c312-4594-b701-4459b4d5d32a` 是历史脏模板：数据库仍有 `templateImageKey`，但文件路径指向不存在的 `storage-local\...`，现在后端会返回 `imageAvailable=false`，前端不会再请求其图片。
3. Python 校正服务此前在角点检测失败时不返回明确结果，现改为返回输出路径或 422 错误原因；成功时仍返回 PNG。

## 7. 未执行范围

- 完整 P1 数据集/项目/报告链路未在本轮执行。
- 浏览器点击级手工流程未截图验证；已用同路径 HTTP 探针验证核心接口。

## 8. 建议后续

### 产品 / 工程

1. 清理或重新上传 `imageAvailable=false` 的历史模板。
2. 为缺图模板在管理页增加“一键替换照片”入口。

### 测试 / CI

1. 增加 `imageAvailable=false` 模板的前端回归用例。
2. 修正后端 `mvn test` 默认 profile 的 DB init 配置，让 CI 能完整执行 SpringBootTest。
