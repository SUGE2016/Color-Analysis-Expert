# 工作日报 · 2026-06-02

## 一句话结论

完成数据集三级路径与真实 API 对接、登录会话与列表/顶栏/分析页 UI 统一；**按项目可配置多阶段 Pipeline（预处理/提取/分析，步骤数与参数各异）尚未实现**，与产品设想仍有明显差距。

## 今日完成

### 后端与数据

- 迁移 `02-dataset-tracking.sql`：学年（`academic_year`）、作品元数据等字段
- 扩展 `DatasetService` / `DatasetController` / `DatasetGroupController`：学年筛选、JWT 鉴权下载、元数据更新
- 登录返回 `token` + `userId` + `username`；`GET /api/auth/me`

### 前端

- 去掉 Mock 登录；`session.js` + `AuthSession`：JWT、401 拦截、路由守卫
- API：`datasetGroup.js`、`dataset.js`（含上传路径）；三级路由 `group → dataset → detail`
- `GroupListPage` / `DatasetListPage` / `DatasetDetailPage` 接真实接口；学年从数据动态生成
- 全局表格：`TableWrap`、`TableActions`、操作列窄屏折叠（详情/报告 pinned）
- 布局：`list-page.module.css`、`PageLayout` 窄屏抽屉侧栏；`Topbar` 共用 `AppLogo`、修复 `SafetyOutlined` 编译错误
- `AnalysisPage` 对齐列表页布局（页头 / 筛选条 / 表格外置分页）

### 工程与其它

- Docker 构建参数、compose 调整；`scripts/bump-build.js`、测试目录 `tests/`（P0/P1 脚本与报告契约）
- 算法服务 `image_correction` 等小改动

## 与设想差距（明日及后续）

| 设想 | 当前实现 |
|------|----------|
| 每个分析项目可独立配置 **预处理 → 提取 → 分析** Pipeline | 分析页仍以 Mock 项目列表为主，创建流未建模可编排步骤 |
| 各项目 **步骤数量、每步参数** 可不同 | 无 Pipeline / Step 的领域模型与前后端 CRUD |
| 运行时可按项目实例执行对应 Pipeline | 后端任务/项目 API 与前端向导未打通 |

**建议后续方向（待排期）**

1. 领域模型：`AnalysisProject`、`PipelineDefinition`、`PipelineStep`（类型 + JSON 参数 schema）
2. 创建/编辑向导：步骤拖拽或表单配置，保存为项目快照
3. 执行层：任务队列按步骤调用 algorithm-service，状态回写分析页

## 已知问题 / 技术债

- 分析项目数据仍为前端 Mock + localStorage 未完成项合并
- JWT 默认 1h，与前端 session 展示需长期对齐策略
- 旧库需手动执行 `02-dataset-tracking.sql`（新 Docker 初始化已含）
- `GroupListPage` eslint：`useMemo` 多余依赖 `availableYears`（warning）

## 测试

- 已有 `tests/scripts/run-p0-p1.sh` 与 `tests/results/` 报告；本次未在日报时点重跑全量 P1
- 默认账号：`admin` / `admin123`

## 明日计划

1. 梳理 Pipeline 产品与数据模型（与现有 `Project`/`Task` API 对照）
2. 分析创建页改为「步骤配置 + 保存 Pipeline 快照」原型或接口草案
3. （可选）清理 `auto-test-group` 重复数据、JWT 过期策略

## Git

- 本次提交拟包含上述前后端与测试脚手架变更；**不包含** `.cursor/` 本地配置
