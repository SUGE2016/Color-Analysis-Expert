# P2 前端手工用例

**前置：** SMK-08；API 可登录；浏览器 Chrome/Edge 最新。

环境变量需指向 API：`REACT_APP_API_BASE_URL=http://localhost:8080/api`（Docker 默认）。

| ID | 页面/模块 | 步骤 | 期望 | 状态 |
|----|-----------|------|------|------|
| UI-01 | 登录页 | 打开 `/login`，输入 admin/admin123 | 跳转主页，localStorage 有 `token` | 部分通过 |
| UI-02 | 错误登录 | 错误密码 | 提示错误，不跳转 | 通过 |
| UI-03 | 注册页 | `/register` 注册新用户 | 成功提示或跳转登录 | 通过（页面） |
| UI-04 | 数据集列表 | 进入数据集模块，创建数据集 | 列表出现新项 | 部分通过 |
| UI-05 | 上传图片 | 在数据集中上传 fixtures 图片 | 列表/预览可见 | 待本机复测 |
| UI-06 | 分析项目 | 创建分析项目并触发 run | 状态变化或成功提示（记录 UI 文案） | 通过（列表） |
| UI-07 | 报告页 | 打开汇总/单图报告 | 有数据展示或明确空态 | 通过 |
| UI-08 | 工具箱 | 边缘检测或图像校正页上传图 | 有结果展示或错误提示可理解 | 通过（页面） |
| UI-09 | 模板管理 | 删除一个仍被项目引用的模板并确认删除 | 后端返回 409；弹窗明确提示模板正在被引用、引用项目数量及处理建议；模板不被删除 | PASS |
| UI-10 | 创建分析-图像矫正 | 选择 2 个各含 1 张图片的数据集和正常模板；进入第二步执行后端矫正；刷新草稿 | 第二步调用项目 correction API 并显示 2/2；服务端可恢复预览；此时不创建 Task、不写回源数据集；Task 仅在最终提交后创建 | 待复测 |
| UI-11 | 分析项目列表-本地草稿去重 | 对同一未完成项目连续自动保存；列表合并历史不同 `temp-*` ID 的同内容草稿；后端出现同名项目 | 新自动保存始终更新同一临时 ID；历史重复草稿只显示最新一条；后端已存在项目时不再显示本地副本 | PASS |
| UI-12 | 数据集-批量图片上传 | 在数据集详情页同时选择多个不同文件名的 `File`；验证普通无名 `Blob` 和显式重命名 | 每个 `File` 按原始 `file.name` 上传；显式文件名优先；只有无名 `Blob` 使用 `upload.png`，不再因全部同名返回 409 | PASS |
| UI-13 | 模板管理-图片完整显示 | 打开模板管理表格和蝴蝶模板详情，检查横图缩略图与详情预览 | 所有模板图片均加载成功；使用 `object-fit: contain` 等比例缩放；横图、竖图和方图均不裁剪、不遮挡 | PASS |
| UI-14 | 新建分析-第四步图片一致性 | 从列表点击“新建分析”，确认初始空选择可正常渲染；再快速依次选择图片 A、B，并让 A 的加载回调晚于 B；检查标题、画布和配置对象 | 初始空选择不访问空加载记录、不出现 React 错误覆盖层；旧加载结果被取消；画布最终只显示 B；标题、画布和 `imageAnalysisConfig` 使用同一图片 ID；加载期间显示状态 | PASS |
| UI-15 | 第四步 V1.0 配置闭环 | 仅给部分图片/区域选择颜色分布；保留一个已选区域无方法后尝试提交；检查出界/线条方法 | 未配置图片不进入提交配置；空方法阻止并定位；颜色分布固定代表熵值、主色、主色数量；出界/线条显示暂不支持且不可选；快速双击只产生一次 run | 待复测 |
| UI-16 | 单图分析报告 | 从汇总报告按 imageId 钻取；点击矫正图区域并使用键盘 Enter/Space；再用下拉框切换；检查三类指标并导出 PDF | 点击、键盘和下拉选择共用当前区域；轮廓高亮与三类固定指标同步；无多边形旧区域仍可用下拉框；不显示原图、样本标签、出界/面积/线条；PDF 下载成功；无运行时错误 | PASS |

## 执行记录

| 执行人 | 日期 | 浏览器 | 通过/总数 | 备注 |
|--------|------|--------|-----------|------|
| Agent | 2026-06-03 | Cursor IDE Browser | 5/8（+2 部分） | 见下 |
| Codex | 2026-07-21 | Codex In-app Browser | 1/1（本次范围） | UI-07：真实汇总、单图、CSV/PDF 导出均通过 |
| Codex | 2026-07-21 | Codex In-app Browser | 1/1（本次范围） | UI-09：被 1 个项目引用的模板删除时，409 中文说明弹窗 PASS |
| Codex | 2026-07-21 | Codex In-app Browser | 1/1（本次范围） | UI-10：第二步同步矫正 2/2，Task 保持 0，源数据集无衍生图写入 |
| Codex | 2026-07-22 | Jest + production build | 1/1（本次范围） | UI-11：草稿去重 3 项单元断言 PASS，生产构建 PASS |
| Codex | 2026-07-22 | Jest + production build | 1/1（本次范围） | UI-12：上传文件名 3 项单元断言 PASS，生产构建 PASS |
| Codex | 2026-07-22 | Codex In-app Browser + Jest | 2/2（本次范围） | UI-13：9/9 模板图片与详情 contain；UI-14：乱序图片加载断言 2/2 PASS，生产构建 PASS |
| Codex | 2026-07-22 | Codex In-app Browser + Jest | 1/1（本次范围） | UI-14：初始空选择及错配保护断言 6/6 PASS；点击“新建分析”正常进入步骤 1，无空对象 `.image` 运行时错误；生产构建 PASS |
| Codex | 2026-07-24 | Codex In-app Browser + Jest | 补充检查 | 新建分析页正常打开，无 `Uncaught`/`TypeError`/空对象读取错误；V1 配置规范化与旧方法清理单测 3/3；UI-15 完整四步交互仍保留待复测 |
| Codex | 2026-07-24 | Codex In-app Browser + Jest + Docker API | 1/1（本次范围） | UI-16：汇总稳定 ID 钻取；矫正图区域点击/键盘/下拉联动三项指标；不展示原图和样本标签；PDF API 有效；无新运行时错误 |

### 2026-06-03 明细

- **环境**：`http://localhost:3001`，API `http://localhost:8080`（P0+P1 已 41 PASS）。
- **UI-01**：登录页渲染正常。Cursor 内置浏览器对 `localhost:8080` 的 `fetch` 失败，无法用 `admin/admin123` 走真实登录；`admin/123456` 可走前端 Mock 进 `/dataset`。注入 JWT 后 `localStorage.token` 可用，但 `LoginPage` 默认只写 `user_session`（与用例「token」字段不完全一致）。
- **UI-02**：错误密码提交后仍停留在 `/login`。
- **UI-03**：`/register` 表单与按钮齐全。
- **UI-04**：「新建分组」弹窗可打开；提交创建在 IDE 浏览器中 API 不可达，列表未验证新增（建议本机 Chrome 复测）。
- **UI-05**：未执行上传（依赖 API + 文件选择器）。
- **UI-06**：注入 token 后 `/analysis` 展示「共 6 个项目」与「新建分析」。
- **UI-07**：`/analysis/{id}/report` 展示「全局汇总报告」与多条「查看详情」。
- **UI-08**：`/toolbox/edge-detection` 上传区与操作按钮可见。

### 2026-07-21 UI-07 明细

- **环境**：Docker Compose，前端 `http://localhost:3000`，API `http://localhost:8080`。
- **数据**：项目 `fb4eae8d-6d4f-40ff-b61f-02bb42c80719`，成功任务 `0121b2d4-a401-43fa-81ab-4bf3c7e2c640`，图片 `02_01_00.jpg`。
- **汇总页**：显示真实项目/任务 ID、1 张图片、主色 49 行、熵值 49 行；预览列表可进入详情。
- **单图页**：图片名正确，主色、主色数量、熵值、边缘颜色共 4 张数据表正常渲染（边缘颜色按接口结果显示空态）。
- **导出**：CSV 与 PDF 均出现成功提示；对应 API 在 RPT-03 中均返回 200。
- **稳定性**：最终页面无 React 运行时错误覆盖层。

### 2026-07-21 UI-09 明细

- **环境**：Docker Compose，前端 `http://localhost:3000`，API `http://localhost:8080`。
- **数据**：模板 `fail-tpl-1784634645`，仍被 1 个项目引用。
- **结果**：确认删除后弹出“模板正在被引用，无法删除”，显示引用数量，并提示先删除相关项目或更换模板；模板未被删除。

### 2026-07-21 UI-10 明细

- **环境**：Docker Compose，前端 `http://localhost:3000`，API `http://localhost:8080`。
- **数据**：2 个数据集各 1 张 `02_01_00.jpg`；正常模板 `auto-tpl-1784634645`。
- **同步校正**：第二步发出 2 次 `POST /api/images/correction/align`；页面显示 2/2 完成，两张 Blob 图片均为 1526×1080。
- **异步边界**：校正后项目 Task 数量仍为 0；仅最终提交分析任务时调用项目 run。
- **数据隔离**：两个源数据集图片数校正前后均为 1，未写入矫正衍生图；临时 draft 验证后已删除。
